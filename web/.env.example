# Copy to .env.local and fill in. .env* is gitignored.

# Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Origin used to build auth callback URLs. Must also be registered in
# Supabase → Authentication → URL Configuration → Redirect URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3100

# NEVER add the service_role key with a NEXT_PUBLIC_ prefix — that ships it to
# the browser and bypasses every RLS policy in the database.

# ---------------------------------------------------------------------------
# WhatsApp Cloud API webhook
# ---------------------------------------------------------------------------
# /api/webhooks/whatsapp is called by Meta, not by a signed-in user, so it has
# no session to check and must prove the caller is Meta by itself.
#
# VERIFY_TOKEN answers Meta's subscription handshake; APP_SECRET verifies the
# HMAC on every delivery. The verify token used to be hardcoded in the route as
# "bachat_book_webhook_secret" — anyone reading the repo could subscribe their
# own app to this URL.
#
# Leave these unset and the route returns 503 rather than accepting anything.
# That is deliberate: a webhook that waves everything through because a variable
# is missing is worse than one that is switched off.
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=

# ---------------------------------------------------------------------------
# Email
# ---------------------------------------------------------------------------
# Only the KEY lives here. Everything else — provider, from name, from address,
# reply-to, and the on/off switch — is edited in Admin Console → Settings, so it
# can change without a deploy.
#
# The key is deliberately NOT in that table: platform_settings is readable by
# any super_admin session, and a stolen admin session should not also hand over
# the ability to send mail as the product. The console reports whether this is
# set and never displays it.
#
# The same value must also be set on the Edge Function
# (Supabase → Edge Functions → daily-digest → Secrets), which is what actually
# sends. Nothing sends while this is empty.
EMAIL_API_KEY=

# Shared secret between pg_cron and the daily-digest function. Set the SAME
# value in the function's secrets, then run once in SQL:
#   select public.schedule_daily_digest('<this value>');
# With it unset the function refuses every request, which is the safe default.
CRON_SECRET=

# ---------------------------------------------------------------------------
# Development seed account
# ---------------------------------------------------------------------------
# The super_admin the seed script creates, used to sign in during development
# and as the "owner" side of the stranger-vs-owner isolation proof in
# db/README.md.
#
# These used to be written into ROADMAP.md, db/README.md and SESSION-START.md
# in plaintext. The docs now name these variables instead, so the value lives
# in one gitignored place and the repo can be public.
#
# NO `NEXT_PUBLIC_` PREFIX, deliberately. That prefix inlines a value into the
# browser bundle — a password there is readable by every visitor. These are read
# only by seed scripts, the screenshot-loop `--login` flag and manual testing,
# never by a component.
#
# Development only. Never point these at an account that holds real data.
SEED_ADMIN_EMAIL=admin@bachatbook.com
SEED_ADMIN_PASSWORD=
